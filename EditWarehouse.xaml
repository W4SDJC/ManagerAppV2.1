<Window x:Class="ManagerAppV2._1.EditWarehouse"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagerAppV2._1"
        mc:Ignorable="d"
        Title="Редактирование склада" Height="55" Width="400" MinHeight="200">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Выберите склад:" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbWarehouses" Grid.Row="1" Margin="0,0,0,10" DisplayMemberPath="Name"
                  SelectionChanged="CmbWarehouses_SelectionChanged"/>

        <Label Grid.Row="2" Content="Новый адрес склада:" Margin="0,0,0,5"/>
        <TextBox x:Name="txtAddress" Grid.Row="3" Margin="0,0,0,15"/>
        <Button Content="Обновить адрес" Background="#FF333333" Foreground="White" Padding="1,1,1,1" Click="BtnUpdateAddress_Click" Grid.Row="4" Height="35" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="105">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>